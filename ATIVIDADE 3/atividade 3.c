#include <stdio.h>
#include <stdlib.h>

//B tree search x,k

int main (){

    i<-1;
    while i <= n[x];
    k >chave[x];
     do i<- i + 1;
    if i<= n[x]
        k = chave[x];
    then return (x,i);

    if folha [x]
        then return NIL
        else DISK-READ(c[x])
        return B-TREE-search(c[x],k)


return 0;
}
